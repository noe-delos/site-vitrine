import BentoGridSection from '@/components/sections/landing-description/description-content';

export default function Description({ dictionary }: { dictionary: any }) {
  return <BentoGridSection dictionary={dictionary} />;
}
